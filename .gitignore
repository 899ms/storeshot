node_modules
.next
out
dist
.DS_Store
*.tsbuildinfo
next-env.d.ts
# Local env / secrets — never commit
.env
.env*.local
# Logs, coverage, OS noise
*.log
coverage/
# Internal planning + scaffolding notes (kept locally, not published)
tasks/
agent/
skills-lock.json
_site/
# Signing / notarization material — never commit
*.p12
*.p8
*.cer
*.mobileprovision
AuthKey_*.p8
.env
.env.*
.env*.local
